export default function SearchBar() {
  return (
    <input
      type="search"
      placeholder="Search (coming in P8)…"
      disabled
      aria-label="Search"
      style={{ marginLeft: 'auto', width: 220 }}
    />
  );
}
