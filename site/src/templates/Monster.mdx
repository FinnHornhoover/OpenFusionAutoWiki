import Dropdown from '../components/Dropdown';
import DropChance from '../components/DropChance';
import EntityLink from '../components/EntityLink';
import Icon from '../components/Icon';
import InlineMeta from '../components/InlineMeta';
import MapSpot from '../components/MapSpot';
import { formatDurationSeconds } from '../data/formatTime';

# {props.data.name}

<div className="entity-header">
  {props.data.icon ? <div className="entity-header-icon"><Icon src={props.data.icon} alt={props.data.name} size={192} /></div> : null}
  <div className="entity-meta muted">
    {props.data.colorType && <span className="badge badge-category">{props.data.colorType}</span>}
    {props.data.level > 0 && <span className="badge badge-category">Level {props.data.level}</span>}
    {props.data.inGame === false && <span className="badge badge-warn">cut from final build</span>}
    {props.data.locations.length > 0 && (
      <span className="badge badge-category">{props.data.locations.length} spawn{props.data.locations.length === 1 ? '' : 's'}</span>
    )}
  </div>
</div>

{props.data.comment && (
  <blockquote className="npc-comment">{props.data.comment}</blockquote>
)}

<dl className="stat-grid">
  {props.data.miscRewards?.taros && (props.data.miscRewards.taros.amount > 0 || props.data.miscRewards.taros.probability > 0 || props.data.miscRewards.taros.odds) && <><dt>Taros</dt><dd>{props.data.miscRewards.taros.amount.toLocaleString()}{props.data.miscRewards.taros.amount > 0 && props.data.miscRewards.taros.odds && props.data.miscRewards.taros.odds !== '1' ? <> ({props.data.miscRewards.taros.odds} chance)</> : props.data.miscRewards.taros.amount > 0 && !props.data.miscRewards.taros.odds && props.data.miscRewards.taros.probability > 0 && props.data.miscRewards.taros.probability < 1 ? <> ({(props.data.miscRewards.taros.probability * 100).toFixed(2)}% chance)</> : null}</dd></>}
  {props.data.miscRewards?.fm && (props.data.miscRewards.fm.amount > 0 || props.data.miscRewards.fm.probability > 0 || props.data.miscRewards.fm.odds) && <><dt>Fusion Matter</dt><dd>{props.data.miscRewards.fm.amount.toLocaleString()}{props.data.miscRewards.fm.amount > 0 && props.data.miscRewards.fm.odds && props.data.miscRewards.fm.odds !== '1' ? <> ({props.data.miscRewards.fm.odds} chance)</> : props.data.miscRewards.fm.amount > 0 && !props.data.miscRewards.fm.odds && props.data.miscRewards.fm.probability > 0 && props.data.miscRewards.fm.probability < 1 ? <> ({(props.data.miscRewards.fm.probability * 100).toFixed(2)}% chance)</> : null}</dd></>}
  {props.data.miscRewards?.potions && (props.data.miscRewards.potions.amount > 0 || props.data.miscRewards.potions.probability > 0 || props.data.miscRewards.potions.odds) && <><dt>Potions</dt><dd>{props.data.miscRewards.potions.amount.toLocaleString()}{props.data.miscRewards.potions.amount > 0 && props.data.miscRewards.potions.odds && props.data.miscRewards.potions.odds !== '1' ? <> ({props.data.miscRewards.potions.odds} chance)</> : props.data.miscRewards.potions.amount > 0 && !props.data.miscRewards.potions.odds && props.data.miscRewards.potions.probability > 0 && props.data.miscRewards.potions.probability < 1 ? <> ({(props.data.miscRewards.potions.probability * 100).toFixed(2)}% chance)</> : null}</dd></>}
  {props.data.miscRewards?.boosts && (props.data.miscRewards.boosts.amount > 0 || props.data.miscRewards.boosts.probability > 0 || props.data.miscRewards.boosts.odds) && <><dt>Boosts</dt><dd>{props.data.miscRewards.boosts.amount.toLocaleString()}{props.data.miscRewards.boosts.amount > 0 && props.data.miscRewards.boosts.odds && props.data.miscRewards.boosts.odds !== '1' ? <> ({props.data.miscRewards.boosts.odds} chance)</> : props.data.miscRewards.boosts.amount > 0 && !props.data.miscRewards.boosts.odds && props.data.miscRewards.boosts.probability > 0 && props.data.miscRewards.boosts.probability < 1 ? <> ({(props.data.miscRewards.boosts.probability * 100).toFixed(2)}% chance)</> : null}</dd></>}
  {props.data.displayHP > 0 && <><dt>HP</dt><dd>{props.data.displayHP.toLocaleString()}</dd></>}
  {props.data.attackPower > 0 && <><dt>Attack power</dt><dd>{props.data.attackPower.toLocaleString()}</dd></>}
  {props.data.power !== 0 && <><dt>Power</dt><dd>{props.data.power.toLocaleString()}</dd></>}
  {props.data.protection > 0 && <><dt>Protection</dt><dd>{props.data.protection.toLocaleString()}</dd></>}
  {props.data.accuracy > 0 && <><dt>Accuracy</dt><dd>{props.data.accuracy}</dd></>}
  {props.data.attackRange > 0 && <><dt>Attack range</dt><dd>{props.data.attackRange}</dd></>}
  {props.data.combatRange > 0 && <><dt>Combat range</dt><dd>{props.data.combatRange}</dd></>}
  {props.data.sightRange > 0 && <><dt>Sight range</dt><dd>{props.data.sightRange}</dd></>}
  {props.data.walkSpeed > 0 && <><dt>Walk speed</dt><dd>{props.data.walkSpeed}</dd></>}
  {props.data.runSpeed > 0 && <><dt>Run speed</dt><dd>{props.data.runSpeed}</dd></>}
  {(props.data.respawnTime || props.data.respawnSeconds > 0) && <><dt>Respawn</dt><dd>{props.data.respawnTime || formatDurationSeconds(props.data.respawnSeconds)}</dd></>}
</dl>


{props.data.locationGroups.length > 0 && (
  <Dropdown summary={`Spawn locations (${props.data.locationGroups.length} group${props.data.locationGroups.length === 1 ? '' : 's'})`}>
    <ul className="ref-list location-list">
      {props.data.locationGroups.map((loc, i) => (
        <li key={i}>
          <MapSpot
            x={loc.x}
            y={loc.y}
            z={loc.z}
            areaId={loc.areaId}
            title={loc.areaZone}
            instanceName={loc.instanceName}
            instanceID={loc.instanceID}
            points={loc.points}
            icon={props.data.mapIcon}
          />
          <span className="muted location-list-meta">
            {loc.spawnCount.toLocaleString()} spawn{loc.spawnCount === 1 ? '' : 's'}
          </span>
          {loc.hp > 0 && loc.hp !== props.data.displayHP && (
            <span className="muted location-list-meta">HP {loc.hp.toLocaleString()}</span>
          )}
        </li>
      ))}
    </ul>
  </Dropdown>
)}

{(props.data.activeSkill || props.data.passiveBuff || props.data.supportSkill) && (
  <Dropdown summary="Skills">
    {props.data.activeSkill && (
      <div className="task-row">
        <span className="task-label">Active:</span>
        {props.data.activeSkillIcon && <Icon src={props.data.activeSkillIcon} alt="active skill" size={80} />}
        {' '}{props.data.activeSkill}
      </div>
    )}
    {props.data.passiveBuff && (
      <div className="task-row">
        <span className="task-label">Passive:</span>
        {props.data.passiveBuffIcon && <Icon src={props.data.passiveBuffIcon} alt="passive buff" size={80} />}
        {' '}{props.data.passiveBuff}
      </div>
    )}
    {props.data.supportSkill && (
      <div className="task-row">
        <span className="task-label">Support:</span> {props.data.supportSkill}
      </div>
    )}
  </Dropdown>
)}


{props.data.drops.length > 0 && (
  <Dropdown summary={`Drops ${props.data.drops.length} item entr${props.data.drops.length === 1 ? 'y' : 'ies'}`} open={props.data.drops.length <= 20}>
    <ul className="ref-list">
      {props.data.drops.map((d, i) => (
        <li key={`${d.item.id}-${i}`}>
          <EntityLink entity={d.item} />
          <InlineMeta leading>
            {d.areaZone && <span>{d.areaZone}</span>}
            <DropChance data={d} leadingSeparator={false} />
          </InlineMeta>
        </li>
      ))}
    </ul>
  </Dropdown>
)}

{props.data.missionsRequiring.length > 0 && (
  <Dropdown summary={`Required for ${props.data.missionsRequiring.length} mission${props.data.missionsRequiring.length === 1 ? '' : 's'}`}>
    <ul className="ref-list">
      {props.data.missionsRequiring.map((m) => (
        <li key={m.id}><EntityLink entity={m} withIcon={false} /></li>
      ))}
    </ul>
  </Dropdown>
)}


<p className="muted alias-ids">Mob Type ID <code>{props.data.id}</code></p>
