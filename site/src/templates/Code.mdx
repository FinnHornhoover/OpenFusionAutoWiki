import EntityLink from '../components/EntityLink';

# Code {props.data.code}

<table className="location-table source-table entity-index-table">
  <thead>
    <tr>
      <th>Item</th>
      <th>Level</th>
      <th>Rarity</th>
    </tr>
  </thead>
  <tbody>
    {props.data.items.map((r) => (
      <tr key={r.ref.id} className={r.obtainable ? undefined : 'entity-index-row-muted'}>
        <td><EntityLink entity={r.ref} iconSize={64} /></td>
        <td>{r.contentLevel > 0 ? r.contentLevel : <span className="muted">-</span>}</td>
        <td>{r.rarity && r.rarity !== 'Any' ? r.rarity : <span className="muted">-</span>}</td>
      </tr>
    ))}
  </tbody>
</table>
