{
  "name": "openfusion-auto-wiki",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "build",
    "mediawiki",
    "site"
  ],
  "scripts": {
    "build:data": "npm run build -w build && node build/dist/index.js",
    "build:site": "npm run build -w site",
    "build:all": "npm run build:data && npm run build:site && npm run build:mediawiki",
    "dev": "npm run dev -w site",
    "preview": "npm run preview -w site",
    "clean": "rm -rf site/dist site/public/data site/public/icons site/public/builds.json build/dist",
    "item-sets:editor": "node scripts/item-set-editor.mjs",
    "build:mediawiki": "npm run build -w mediawiki && node mediawiki/dist/index.js",
    "publish:mediawiki": "npm run build -w mediawiki && node mediawiki/dist/publish.js",
    "prepare:mediawiki": "npm run build -w mediawiki && node mediawiki/dist/prepare.js",
    "package:mediawiki": "bash mediawiki/local/package-release.sh",
    "mediawiki:local": "docker compose -f docker-compose.mediawiki.yml up -d --build mediawiki",
    "mediawiki:local:down": "docker compose -f docker-compose.mediawiki.yml down",
    "mediawiki:local:reset": "docker compose -f docker-compose.mediawiki.yml down --volumes",
    "mediawiki:snapshot": "bash mediawiki/local/snapshot-host.sh",
    "mediawiki:restore": "docker compose -f docker-compose.mediawiki.yml exec mediawiki ofaw-mediawiki-restore",
    "mediawiki:publish-local": "bash mediawiki/local/publish-local.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
